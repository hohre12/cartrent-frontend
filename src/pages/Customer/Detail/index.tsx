import styled from 'styled-components';

const CustomerDetail = () => {
  return <DetailWrapper>고객 상세</DetailWrapper>;
};

export default CustomerDetail;

export const DetailWrapper = styled.div`
  background: #fff;
`;
