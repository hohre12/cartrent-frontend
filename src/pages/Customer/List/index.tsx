import styled from 'styled-components';

const CustomerList = () => {
  return <ListWrapper>고객 리스트</ListWrapper>;
};

export default CustomerList;

export const ListWrapper = styled.div`
  background: #fff;
`;
